import { useSelector } from "react-redux"

export default function UserName() {
  const {} =useSelector((res)=>res.)
  return (
    <p className=" hidden uppercase text-stone-100 font-semibold text-sm md:block">  </p>
  )
}
